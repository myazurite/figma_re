var elem = document.querySelector('.grid');
var flkty = new Flickity( elem, {
    // options
    cellAlign: 'left',
    contain: true,
    draggable: true,
    imagesLoaded: true,
    wrapAround: false,
    pageDots: false,
    prevNextButtons: false,
});

